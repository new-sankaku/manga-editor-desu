# レビューチェックリスト

ユーザーから修正ポイントの確認を求められた際に見落とされた項目を蓄積する。
同じ問題が繰り返される場合は回数をインクリメントして優先度の目安とする。

**上限100項目。超過時は回数0の古い項目から削除する。**

## チェック項目

| # | カテゴリ | チェック内容 | 回数 |
|---|---------|-------------|------|
| 1 | DOM | innerHTML代入時にサニタイズしているか | 0 |
| 2 | DOM | テンプレートリテラルで組み立てたHTMLにユーザー入力が混入しないか | 0 |
| 3 | 保存 | 追加したパラメータがSETTINGS_SCHEMA/BASEPROMPT_SCHEMAに含まれているか | 0 |
| 4 | 保存 | 新規オブジェクト属性をcommonPropertiesに追加したか | 0 |
| 5 | i18n | 新規UI文言にi18nキーを設定し8言語分の翻訳を追加したか | 0 |
| 6 | 履歴 | 一時オブジェクトにsetNotSave/excludeFromLayerPanelを設定したか | 0 |
| 7 | UI | 固定幅(px)ではなくフレキシブルレイアウトにしているか | 0 |
| 8 | UI | 既存の表示・表記と統一されているか | 0 |
| 9 | fallback | エラー時にfallback値で動作を続行していないか（fallback禁止） | 1 |
| 10 | file:// | file://プロトコルで動作するか（fetch等HTTP前提APIを使っていないか） | 0 |
| 11 | ログ | console.logではなくSimpleLoggerを使っているか。デバッグ時もlogger.trace等を使いログレベル変更で対応する | 1 |
| 12 | 命名 | 変数名がcamelCaseか、APIレスポンスのキーを変換していないか | 0 |
| 13 | format | npm run formatで崩れるスペースを入れていないか | 0 |
| 14 | GUID | 新規オブジェクトにguidを付与しているか | 0 |
| 15 | イベント | EventDelegatorのパターンに従っているか（直接addEventListenerしていないか） | 0 |
| 16 | 履歴 | ループやcanvas.add/remove連続呼び出しでsaveStateByManual()が都度発火しないか。都度発火すると大量の履歴スナップショットが生成され、メモリの圧迫・履歴保存による画面の固まり・Undo/Redoで無意味な操作が発生する。複数操作はwithoutHistory(fn)で囲み最後に1回だけ保存（同一タスク内のイベントは自動集約されるが、非同期をまたぐ処理は集約されない） | 1 |
| 17 | 性能 | forループ等でオブジェクトのプロパティを連続変更する際、毎回canvas.renderAll()を呼んでいないか。都度呼ぶとオブジェクト数×ループ回数分の再描画が走りUIがフリーズする。ループ外で最後に1回だけ呼ぶ | 1 |
| 18 | ログ | Errorオブジェクトを文字列化する際にJSON.stringifyを使っていないか。Errorのmessage/name/stackは非列挙プロパティのためJSON.stringifyでは`{}`になる。`instanceof Error`で判定し`error.name + error.message`等で展開する | 1 |
| 19 | ログ | 各ファイルで`new SimpleLogger()`していないか。ロガーは`js/core/logger.js`に集約定義し、各ファイルではグローバル変数として参照する | 1 |
| 20 | 非同期 | `_comfyUIExecProvider`等のグローバル変数に依存するURL/認証情報を、長時間のawait（WebSocket待機等）をまたいで使っていないか。非同期処理中に別タスクがグローバル変数を上書きし、別プロバイダのURLに接続してしまう。関数冒頭でサーバーアドレス・認証情報をローカル変数にキャプチャして使う | 2 |
| 21 | fallback | グローバル変数が未設定のときデフォルト値（`'local'`等）にフォールバックしていないか。`_comfyUIExecProvider`がnullのときキーを`'local'`にする、`comfyObjectInfoListMap.get(key)\|\|comfyObjectInfoListMap.get('local')`のように別キーにフォールバックする等はすべて暗黙のfallback。awaitなしで呼ばれたasync関数ではグローバル変数が既にクリアされているため、呼び出し元で値を事前キャプチャして引数で渡す | 1 |
| 22 | 履歴 | オブジェクト属性を`obj.left=100`のように直接代入するUIハンドラで`commitHistory()`/`commitHistoryDebounced()`を呼んでいるか。canvasイベントが発火せず、自動コミット網（`_set`フック）も直接代入は検知できないため履歴に残らず、次のUndoが1つ前の操作まで巻き戻る。連続入力（スライダー・キーリピート）は必ずdebounce版を使う | 1 |
| 23 | 履歴 | `changeDoNotSaveHistory()`の解除漏れがないか。非同期コールバックをまたぐ場合はtry/finallyで囲む。解除漏れは以降の全操作が履歴に残らなくなり、Undoが大きく巻き戻る。同期処理は`withoutHistory(fn)`を使う | 1 |
| 24 | 履歴 | 一時的なUI用オブジェクト（クロップ枠等）に`setNotSave`/`excludeFromLayerPanel`/`excludeFromExport`を設定し、`addByNotSave`/`removeByNotSave`で追加削除しているか。通常のadd/removeでは無意味な履歴が積まれる | 1 |
| 25 | 履歴 | 画像を差し替える効果（glfxフィルタ等）で、非同期の完了後にコミットしているか。`setElement()`は`set()`を通らないため自動コミット網で検知できず、抑止スコープも非同期コールバックには効かない。プレビューのまま履歴に残らないと次のUndoが1つ前の操作まで巻き戻る | 1 |
| 26 | 保存 | プロジェクトファイルに新しいjsonを追加するとき、履歴状態の読み込み対象に混入していないか。状態判定は`state_`プレフィックスで行う。読み込み側は`loadLz4BlobProjectFile()`と`generation-task-manager.js`の2箇所あり、片方だけ直すと裏側のAI処理でだけ壊れる | 1 |
| 27 | ページ | ページ切り替え・削除・追加・auto-saveで`isProjectBusy()`を確認しているか。読み込み中に保存すると、読み込み途中のキャンバスを現在ページのblobに上書きしてページが壊れる | 1 |
| 28 | ページ | ページを離れる前に`flushHistory()`しているか。デバウンス中の変更が確定する前に離れると変更が失われる | 1 |
| 29 | i18n | 動的生成するダイアログの文言をハードコードしていないか。`innerHTML`で組み立てる場合も`getText()`を通す | 1 |
| 30 | AI | 設定ウィンドウを開かないと初期化されない処理に依存していないか。ComfyUIのObjectInfoは設定ウィンドウを開くまで取得されず、起動直後は生成・背景削除が一律に失敗していた。必要な時点で取得すること | 1 |
| 31 | AI | ロール判定（`hasNotRole`）に渡すロールが処理内容と一致しているか。T2IボタンにImage2Image、I2IボタンにText2Imageが指定されていた | 1 |
| 32 | 非同期 | `while(true)`の監視ループ内で例外を握りつぶしているか。握りつぶさないとループごと終了し、以降その監視が二度と動かない | 1 |
| 33 | UI | 選択オブジェクトの値をパネルに反映しているか。反映しないと表示中の古い値が残り、スライダーを少し動かしただけで別の値に飛ぶ。`syncObjectControls()`を使う | 2 |
| 34 | UI | jscolorのピッカーを`value`代入で更新していないか。見た目が変わらないため`picker.jscolor.fromString()`を使う | 1 |
| 35 | UI | `oninput`のハンドラが対象と一致しているか。吹き出しの色入力に`changePanelStrokeColor`が指定されており、吹き出しには無反応でコマ選択中はコマの線色が変わっていた | 1 |
| 36 | UI | 条件を満たさず処理しないとき、画面を空欄のまま残していないか。理由を表示しないと操作ミスか処理中か判断できない | 1 |
| 37 | file:// | 実行に必要な外部ライブラリを`third/`に同梱しているか。CDN参照はオフラインで失敗する | 1 |
| 38 | 性能 | 一覧を全件まとめて生成していないか。ブレンドは25モード分のプレビューを毎回生成していた。開いている範囲だけ生成する | 1 |
| 39 | UI | 画面座標へ変換するとき、表示倍率を状態変数ではなくDOMから実測しているか（`getCanvasDisplayScale()`）。CSSのscaleと状態変数がずれると位置が狂う | 1 |
| 40 | UI | はみ出し判定をビューポート基準で行っているか。キャンバス基準だと拡大時にキャンバス右端が画面外でもクランプされず、要素が画面外に出る | 1 |
| 41 | UI | スクロール対象の要素を取り違えていないか。`overflow:hidden`の要素に`scrollLeft`を代入しても何も起きない | 1 |
| 42 | UI | スクロール領域内で`transform:scale()`する要素の`transform-origin`が`top left`か。中央基準だと左と上のはみ出しに到達できない | 1 |
| 43 | 保存 | 破壊的操作のバックアップにauto-saveを使っていないか。auto-saveは`pages`/`metadata`の**単一スロットを毎回上書き**し世代を残さない。スキップ判定も表示中ページの履歴状態ハッシュ基準のため、操作後は必ず走って潰れる。**復元導線は起動時ダイアログのみで、任意のタイミングで押せるボタンは存在しない**。戻せる必要があるなら専用ストアへ退避し復元ボタンを用意する | 1 |
| 44 | 性能 | ループで回す処理の中で`fx.canvas()`のようなWebGLコンテキストや`texture()`を毎回新規生成していないか。コンテキストはブラウザ上限（16程度）で古いものがロストし途中から結果が壊れる。textureはGPU上に残り解放されない。1個を使い回し`loadContentsOf()`で詰め替える | 1 |
| 45 | 履歴 | 非同期をまたぐ一括処理で`withoutHistory(fn)`を使っていないか。`withoutHistory`は同期関数用でawaitを待たないため抑止が先に解除される。`changeDoNotSaveHistory()`〜try/finally〜`changeDoSaveHistory()`を使う | 1 |
| 46 | エラー | 一括処理で1件の失敗を握りつぶして次へ進んでいないか。どこまで適用されたか分からなくなる。打ち切って「何件目まで完了したか」を必ず表示する。呼び出し元がawaitしないasync関数はcatchを持たせないとunhandled rejectionになり、オーバーレイだけ消えて無言で終わる | 1 |
| 47 | ページ | 「中身があるか」の判定を履歴件数（`stateStack.length>=2`）で代用していないか。キャンバスのリサイズや背景色設定だけでも履歴は積まれるため、起動直後の空白ページがページとして保存され、ボトムバーに空サムネイルが増える。判定は`btmShouldSaveCurrentPage()`に集約し、キャンバス上のオブジェクト有無で判断する | 1 |
| 48 | ページ | 空判定でキャンバス上のUI専用オブジェクト（初期メッセージ`isInitMessage`等）を除外しているか。除外しないと空ページでも「中身あり」と誤判定する | 1 |
| 49 | ページ | `chengeCanvasByGuid()`のawait後にキャンバスの中身を数えていないか。内部の`lastRedo()`→`applyHistoryState()`は`canvas.loadFromJSON()`をコールバックで呼ぶだけで完了を待たないため、awaitが返った時点では**オブジェクトが0件**。エラーも例外も出ず「ページが切り替わるだけで何も起きない」になる。`isProjectBusy()`がfalseになるまで待つ | 1 |
| 50 | 既存API | 既存関数を書き換えるとき、旧名で呼んでいる箇所を全部追ったか。`C2BWStartLight`等を消したのに`effect-manager.js`が呼び続けており、選択画像スコープがReferenceErrorで無反応になっていた。`grep -rn "旧関数名" js/`で確認する | 1 |
| 51 | 仕様確認 | そのアプリで「ロック」「非表示」等が実際にどのプロパティで表現されているか確認したか。このアプリのロックは`selectable`で、`lockMovementX`等ではない。fabricの一般的な属性を当て推量で使うと条件が永久に成立しない | 1 |
| 52 | i18n | `data-i18n="[title]xxx"`のような属性翻訳記法を使っていないか。`updateContent()`は`element.innerHTML = translation`しかせず属性に対応していないため、キー文字列がそのまま本文に差し込まれ`material-icons`のリガチャが壊れる。属性の説明文は`data-tip`を付けて`addTooltipsByAttribute()`（tippy）に載せる | 1 |
| 53 | UI | サイドバーパネルの`left_area`に`style="display:none"`を付けたか。付け忘れるとDOMContentLoadedで`toggleVisibility()`が走るまでパネルが開いた状態で描画される | 1 |
| 54 | UI | 一覧と設定を同じ縦列で場所を奪い合わせていないか。パネル幅200pxに一覧と操作盤を同居させると、一覧に`max-height`を付ければ二重スクロール、付けなければ設定が画面外に押し出される。一覧は内容分だけ、設定は残り全部という配分にする | 1 |
| 55 | UI | 押した後の挙動が違うボタンを同じ見た目で並べていないか。効果は即適用・設定後Submit・プレビュー付き・別ウィンドウの4通りがあり、見分けが付かないと結果が予測できない。グループ見出しと印（`preset-item-mark`）で押す前に示す | 1 |
| 56 | UI | 適用範囲のような「対象を選ぶUI」を、それが効かない項目まで含む位置に置いていないか。効果パネルの適用範囲は変換グループにしか効かないため`preset-section`の囲みの内側に入れる | 1 |
| 57 | i18n | `innerHTML`で後から差し込んだ`data-i18n`要素を翻訳したか。`applyLabelTranslations()`は`data-i18n-label`専用で`data-i18n`を見ない。差し込み後に`updateContent()`を呼ぶ | 1 |
| 58 | i18n | 他要素から表示名を写している要素を、言語切替後に取り直しているか。`updateContent()`は`data-i18n`を持つ要素しか更新しないため、写した値は前の言語のまま残る | 1 |
| 59 | 重複定義 | 呼ぼうとしているグローバル関数が複数ファイルで定義されていないか。`updateContent`は`i18next.js`とprompt-helper内に同名で存在する（後者はクロージャ内なので非グローバル）。`grep -rn "function 関数名" js/`で確認する | 1 |
| 60 | 保存 | その退避は本当に永続化が必要か検討したか。「直前の操作を取り消す」だけならメモリ上の変数で足り、リロードで消えて構わない。永続化するとプロジェクトごとのスロット管理・世代上限・掃除の導線が芋づるで必要になり、消し忘れたデータがブラウザに残り続ける。永続化しないと決めたらツールチップ等でユーザーにも明示する | 1 |
| 61 | 保存 | 複数プロジェクトを跨ぐ保存で、プロジェクトを識別しているか。固定キー1スロットだと別プロジェクトを開いている間も復元でき、押した瞬間に現在のプロジェクトが別プロジェクトの内容で丸ごと置き換わる。このアプリはページGUID（`canvasGuid`）が`commonProperties`経由でプロジェクトファイルに保存されるので識別に使える | 1 |
| 62 | UI | 選択オブジェクトの反映処理で「種類が同じなら何もしない」分岐になっていないか。同じ種類でも中身は別物のため、値を入れ替えないと前に選んでいたオブジェクトの値が表示され続ける。アアアを選んだあとイイイを選んでもサイドバーがアアアのままだった | 1 |
| 63 | 保存 | オブジェクトに持たせる設定スナップショットを「利用者が触った項目だけ」から作っていないか。`sidebarValueMap`は変更のあったidしか持たないため、未変更の項目が欠けたスナップショットになり、選び直したときにその項目だけ前のオブジェクトの値が残る。DOMの入力要素から集める | 1 |
| 64 | UI | 「選ぶ」操作が生成・実行まで行っていないか。押すたびに既定位置へ新規作成する作りだと、同じ座標に重なって増えるため見た目が変わらず、履歴とレイヤーだけが増える。選ぶ操作と挿入操作を分ける | 1 |
| 65 | DOM | 利用者の入力をテンプレートリテラルで`<textarea>`の中身として埋め込んでいないか。`</textarea>`を含む入力でDOMが壊れる。描画後に`element.value`へ代入する | 1 |
| 66 | 仕様確認 | 実現手段を決める前に、同じ目的の既存機能を探したか。「プロンプトを設定する機能」に対し、既に`prompt/llm/`のLLM生成基盤と「ネームから一括生成」があるのに手入力欄を作ってしまった。同じ領域に既存実装がある場合はまずそれに載せられないか検討する | 1 |
| 67 | i18n | チェックボックスやアイコンを含む要素に`data-i18n`を付けていないか。`updateContent()`は`innerHTML`ごと差し替えるため、中の`<input>`や`<i>`が消える。内側に`<span data-i18n>`を置く | 1 |
| 66 | i18n | 後から差し込んだDOMに翻訳を当てる処理が、`document`全体を走査して`textContent`で上書きしていないか。フォント管理モーダルとImage Prompt Helperがこれをやっており、開いた瞬間に他モジュールのボタンまで巻き込んで`&#128295; 使用モデル・ワークフロー`のように実体参照が生のまま表示されていた。走査は挿入したコンテナ配下に限定し、`updateContent()`と同じく`innerHTML`で入れる | 1 |
| 68 | 保存 | API取得後に`<option>`が作られるselectを、保存値の復元対象にしていないか。`el.value=保存値`はマッチするoptionが無いと無言で`''`になり、設定の自動保存が全項目を一括保存するため**localStorage側の保存値まで空で潰れる**。Grokのモデル選択がリロード後に復帰しなくなっていた。復元は`applySettingValue()`（option補完付き）を使い、一覧の再取得側も取得失敗時に選択値を空にしない | 1 |
| 69 | AI | モデル一覧の自動取得条件を「ロール割り当て済み」だけにしていないか。APIキーだけ入れてロール未割り当ての状態では一度も取得されず、設定ウィンドウのモデル欄が空・disabledのまま「再取得」を押すまで直らない。設定ウィンドウを開いたときは接続情報が入っているプロバイダを`{silent:true}`で取得する | 1 |
| 70 | 根拠 | 「なぜそれが効くのか」を、確認していない仕組みで説明していないか。**LLMが「前から取って止める」から並び順が要る**（LLMはそんな動作をしない）、**Danbooruの件数＝タグの効き目の強さ**（件数が示すのは学習されているかとモデルの既定値だけ。効き目はタグの意味の狭さで決まる）、**3000トークンで指示追従が劣化する**（検索で出たブログの、長文入力の推論性能の話を確認せずに適用した）。もっともらしい機構を作って設計の根拠にすると、そこから派生する仕様が全部間違う。確認できないなら「効果は未検証」と書く | 3 |
| 71 | LLM | プロンプトに具体的なタグ・単語を例として並べていないか。LLMは「使うべきものの一覧」と受け取り**全出力に入れる**。`buildings, sky, road`と書けば室内の場面に`sky`が入り、効果線を列挙すれば寄りのコマ全部に効果線が入り、`{"index":1,"role":"establishing"}`という出力例を書けば1コマ目がestablishingに固定される。判定は「そのタグが大半の出力に出たとき、意図通りか壊れているか」。例を出すなら件数付きの参照表にして「選択肢ではない」と明記する。**一度塞いでも再発しやすい** | 2 |
| 72 | LLM | プロンプトに根拠のない固定値を書いていないか。「最大6キャラ」「1コマ15〜40タグ」「4コマ以上なら人物なしを1枚」。しかも上限は**超過分を黙って捨てる**ためfallback禁止に反する（7人目のキャラがどこにも表示されず消える）。上限ではなく「何を作る対象とするか」の判断軸を渡す | 1 |
| 73 | LLM | プロンプトに書くタグが実在するか確認したか。`biting lip` `hand over mouth` `teary eyes` `arms crossed` `sweating profusely` `hand out of frame`はDanbooru 0件、`downcast eyes`は202件で効かない。**存在しないタグは効かないだけでなく、効いているつもりになるので害がある。** `https://danbooru.donmai.us/tags.json?search[name_comma]=tag1,tag2`で件数を確認する | 1 |
| 74 | LLM | プロンプトが文学的な言い回しになっていないか。「読者が手に持って帰るもの」「握った拳と真顔のほうが怒鳴り顔より怒って見える」では何をすればいいか分からない。**読んでそのまま手が動く形**にする。判断させる項目には「何を見て決めるか」と「外したら何が起きるか」を必ず添える（→ `prompt-composition.md` 判断軸の章） | 2 |
| 75 | LLM | システムプロンプトとユーザーメッセージで違うことを言っていないか。システム側を「フレームに入る範囲だけ取れ」に変えたのに、ユーザーメッセージのラベルが`copy these tags verbatim`のまま残り、顔だけのコマにも靴が入っていた。手順に「2/3〜3/4に置く」と固定で書いた直後に「転から始めてもよい」を足して矛盾させた。**片方を直したら、同じことを言っている全箇所を`grep`で洗う** | 2 |
| 76 | 説明 | 表にまとめて要約していないか。セルに収めると各項目が結論だけになり、何が起きているのか読み取れない。**実際の入力・実際の出力・実際に送っている文字列をそのまま並べる。** 動かして取った値を使い、書き直さない | 1 |
